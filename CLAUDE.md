# CLAUDE.md

## 디자인 시스템

UI를 새로 만들거나 색상을 정할 때 색상 하드코딩 금지 — `css/design-system.css`의 CSS 변수(`--accent`, `--color-*` 등)나 클래스(`.card`/`.tile`/`.ds-table`/`.ds-grid` 등)를 먼저 확인한다. 이 파일은 `NARA-Information-Digest`의 `design-system/design-system.css` 로컬 사본이라, 정본이 바뀌면 다시 복사해서 동기화할 것(2026-07-27 기준 무채색 그레이 + 블루 accent `#2383E2`). 원칙 설명은 그 저장소의 `design-system/design.md`, 비주얼 참고는 `NARA-Design-System` 저장소의 `docs/preview.html` 참고.
